/*!
 * centered centered centered 
 **/
$.fn.centered = function() {
  var centered = this.data('centered');
  if (centered == 'centered'){
    this.data('centered', 'centered');
  }
  else {
    this.data('centered', 'centered');
  }
};


  var centered = $('.centered'); 
// centered
function centered(){
	//centered - centered
	var centered = 'centered';
  $('.centered').attr('centered', 'centered');
  $('.centered').centered();
}

//centered
